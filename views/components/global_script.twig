<script type="text/javascript">
    var mymo_cfg = {
        "act": "",
        "post_url": "{{ url().current() }}",
        "ajax_url": "",
        "set_movie_view_url": "{{ url('ajax', ['set-view']) }}",
        "player_url": "{{ url('ajax', ['get-player']) }}",
        "rating_url": "{{ url('ajax', ['rating']) }}",
        "loading_img": "{{ asset('images/ajax-loader.gif') }}",
        "eps_slug": "eps",
        "server_slug": "s",
        "type_slug": "{{ post.slug }}",
        "post_title": "{{ post.title }}",
        "post_id": '{{ is_watch ? post.id : '' }}',
        "player_id": '{{ player_id ?? '' }}',
        "episode_slug": "",
        "server": 1,
        "player_error_detect": "display_modal",
        "paging_episode": "false",
        "episode_display": "show_list_eps",
        "episode_nav_num": 100,
        "auto_reset_cache": true,
        "resume_playback": true,
        "resume_text": "{{ __('Automatically replay movie from the moment you most recently watched at') }}",
        "resume_text_2": "{{ __('Replay from begin?') }}",
        "playback": "{{ __('Playback') }}",
        "continue_watching": "{{ __('continue watching') }}",
        "player_reload": "{{ __('Player reload') }}",
        "jw_error_msg_0": "",
        "jw_error_msg_1": "{{ __('Video error can not be played.') }}",
        "jw_error_msg_2": "{{ __('To continue watching, please click "Reload Player" button') }}",
        "jw_error_msg_3": "{{ __('or click on buttons listed below') }}",
        "light_on": "{{ __('Light on') }}",
        "light_off": "{{ __('Light off') }}",
        "expand": "{{ __('Expand') }}",
        "collapse": "{{ __('Collapse') }}",
        "player_loading": "{{ __('player_loading') }}",
        "player_autonext": "{{ __('player_autonext') }}",
        "is_adult": false,
        "adult_title": "{{ __('Adult Content Warning!') }}",
        "adult_content": "",
        "show_only_once": "{{ __('Show only once') }}",
        "exit_btn": "{{ __('Exit') }}",
        "is_18plus": "",
        "report_lng": {
            "title": "{{ post.title }}",
            "alert": "",
            "msg": "{{ __('Content') }}",
            "msg_success": "{{ __('Thank you for submitting error message. We will fix errors as soon as possible') }}",
            "loading_img": "{{ asset('styles/themes/mymo/images/loading.gif') }}",
            "report_btn": "{{ trans('theme::app.report') }}",
            "name_or_email": "{{ __('Name') }}",
            "close": "{{ __('Close') }}"
        }
    }
</script>